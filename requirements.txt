# Django Core (pinned to the installed major; do not auto-jump to 6.x untested)
Django>=5.2,<6.0

# Environment Variables
python-dotenv>=1.0.0

# Static file serving in production (DEBUG=False). Required so Django/admin
# assets are served by the app itself — works on PythonAnywhere even without
# a Web-tab static mapping.
whitenoise>=6.6.0
# gunicorn>=21.2.0  # not needed on PythonAnywhere (it provides the WSGI server)

# Email Backends (Optional - install based on your provider)
# SendGrid
# django-sendgrid>=6.2.0

# Mailgun (via Anymail)
# django-anymail>=10.0

# Amazon SES
# django-ses>=3.5.0
# boto3>=1.28.0

# SMS (Optional - Twilio)
# twilio>=8.10.0

# Database (Optional - for production)
# psycopg2-binary>=2.9.0  # PostgreSQL
# mysqlclient>=2.2.0  # MySQL

# Task Queue (Optional - for production email campaigns)
# celery>=5.3.0
# redis>=5.0.0

# Security
# django-ratelimit>=4.1.0
# Brute-force protection (optional upgrade — the project ships with an
# equivalent built-in: account lockout + IP tracking + LoginSecurityLog).
# To use django-axes instead, install it and add 'axes' to INSTALLED_APPS,
# 'axes.middleware.AxesMiddleware' to MIDDLEWARE, and the AxesBackend to
# AUTHENTICATION_BACKENDS, then run migrations.
# django-axes>=6.4.0

# Development Tools (comment out in production)
# django-debug-toolbar>=4.2.0
